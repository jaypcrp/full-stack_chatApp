apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: chat-app
spec:
  selector:
    app: frontend
  ports:
  - port: 80 # The port on which service listens.
    targetPort: 80 # The port on which pod listens.
